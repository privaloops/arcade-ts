// @sprixe/frontend — arcade UI entry point.
// Phase 1 scaffold — real screens land in the following sub-steps.

const app = document.getElementById("app");
if (app) {
  app.textContent = "Sprixe Arcade Frontend — Phase 1 skeleton";
}
